<h1> Travel Goals </h1>

<ul>
    <% @goals.each do |goal| %>
        <li><%= link_to goal.name, goal_path(goal) %></li>
    <% end %>
</ul>

<!-- 
<%= button_to "See Passport", action: "index", controller: "destinations" %> 
-->